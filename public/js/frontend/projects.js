// // Projects grid
// (function () {

//   const grid = document.getElementById('projects-grid');
//   if (!grid) return;

//   const iconItems = document.querySelectorAll('.icon-wrap');
//   iconItems.forEach( (item) => {
//     item.innerHTML = window.Icons[item.dataset.icon] || window.Icons.code;
//   })

//   document.querySelectorAll('.github-icon').forEach( (github) => {
//     github.innerHTML = window.Icons.github;
//   });

//   const liveIcon = document.querySelectorAll('.live-demo').forEach( (live) => {
//     live.innerHTML = window.Icons.external;
//   });
// console.log(window.Icons);
// })();
